# Appendix — Design artifacts

Location for diagrams, schemas and example payloads referenced from `design.md`.

Folders
- `diagrams/` — mermaid files and image exports
- `schemas/` — JSON schema or SQL DDL files
- `examples/` — example payloads or test fixtures

TODO: add files as design work progresses.