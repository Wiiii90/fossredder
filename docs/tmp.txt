also erstmal hätte ich zwischendrin gerne änderungen:
1) wenn ich aktuell auf ein statement klicke, dann steht im hauptteil "booking group view" aber sollte "statement view", wenn ich eine bookinggroup auswähle dann steht da "bookinggroup view" aber sollte transactionview und bei transaction steht da "yearly statement view" sollte "transaction view" sein und eigentlich sollte darüber noch ein yearly statement level sein wobei sich dieser über jahreszahlen gut abtragen lässt, aber hierzu punkt 2) beachten.
2) ich will eigentlich den tree anders strukturiert haben: unser atom ist ein spezieller tag, also ein datum zb 28.04.2022 und alle unsere vier modelle haben eine direkte zuordnung zu einem oder mehreren tagen, wobei eine transaktion genau einem tag zugeordnet ist, eine buchungsgruppe entsprechend ihrer transaktionen an mehreren tagen und ein statement entsprechend ihrer buchungsgruppen entsprechend ihrer transaktionen ebenfalls an mehreren tagen, wobei statements und vll sogar buchungsgruppen zb die grenze einer jahresabrechnung überschneiden können, während eine jahresabrechnung einfach den gesamten bereich eines jahres darstellt (entsprechend juristischer gegebenheiten). es sollte also im tree vll an obersten stelle als root dann jahreszahlen stehen wobei diese dann einer abrechnung zugeteilt werden und in ihren branches sind dann komplette tage, also zb 28. April, wobei man dann zu diesem datum sozusagen sowohl das statement, bookinggroup, als auch transaktion öffnen kann (die dann in entsprechender view angezeigt wird im hauptteil). der tree gleicht dann also eher einem chronologischen zeitstrahl und ermöglicht einem trotzdem eine sehr direkte navigation, wobei sich der zoom ja vor allem aufs hauptpanel bezieht und sich die kontextactions jetzt eben vor allem dann ändern wenn man auf ein jahr klickt (jahresabrechnung) oder wenn man eines von den statement, bookinggroup, transaction ausgewählt hat, wobei diese sehr viel ähnlicher sind, da sie auf transaktionen hinauslaufen (man kann also zb eine analyse in auftrag geben über die toolbar, wobei die analyse so oder so über [alle] transaktionen [der menge] läuft) oder zb bei der jahresabrechnung sowas eine vollständige jahresabrechnung generieren lassen (wofür es dann controller cockpit und so weiter geben wird analog zum importcontroller zb wo neue statements eingepflegt werden, aber das ist zukunftsmusik)
3) bitte nutz statt YearlyStatement in zukunft nur noch Annual als namen also AnnualView z.B. und konsistent überall (sowohl bei den namen der dateien als auch bei den namen im code) ändern sowie alte überbleibsel löschen (von yearlystatement soll nichts mehr zu lesen sein)


leider lagt es immer noch. dieses problem ist erst eingetreten, seit wir diese logos rendern (bzw generell da am rendering etwas vorgenommen haben). außerdem gibt es da probleme, wenn ich den baum komplett aufklappe, dann ist auf transaktionsebene nur son abgebrochener text zu sein "t" und so ein halbes "x", wobei darüber bg-2020- als kategorie steht, diese also auch abhakt ist (aber an anderer x koordinate als die tx) und diese trennung ist irgendwo auf mittiger höhe, was es halt echt hässlich macht. aber das problematischste sind die lags. vll sollten wir als logo auch überall außer bei jahr einfach diese weiße datei wählen wie bei den tagen.
außerdem hast du etwas falsch verstanden gehabt bei der strukturierung des baumes: dieser TAG also 2020-01-03 zb soll dem level eines statements also eines bankauszugs entsprechen, weil niemals 2 bankauszüge auf einen tag zugeordnet sind (zumindest gehen wir jetzt einfach davon aus), also als text könnte da stehen "3. Januar | Bankauszug von 1.1.2022 bis 29.1.2022" -> {Buchungsgruppe von 1.1.2022 bis 4.1.2022, Buchungsgruppe von ..., ...}. und nun soll sich entsprechend beim klick aufs jahr (root) die annualview öffnen im hauptpanel (oder wie das genannt wird), wenn man auf den tag klickt mit bankauszug, dann kommt man auf die statementview, bei buchungsgruppe auf die buchungsgruppen view und zum schluss kommen ja noch die entsprechenden transaktionen, wobei die erstmal einfach nummeriert sein können (nummiert innerhalb der bankauszüge) und eben dann den buchungsgruppen zugeteilt sind 