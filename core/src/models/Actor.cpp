#include "core/pch.h"
#include "core/models/Actor.h"

Actor::Actor(const std::string& name, const std::string& type, const std::string& description)
    : name(name), type(type), description(description) {
}